apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: mlops
spec:
  type: NodePort
  selector:
    app: minio
  ports:
    - name: http
      port: 9000
      targetPort: http
      nodePort: 30000
    - name: console
      port: 9001
      targetPort: console
      nodePort: 30001